schema_version = "1.0.0"

id = "smart_cache_sequencer"
name = "Smart Cache Sequencer"
version = "1.0.25"
tagline = "AE-inspired disk caching for VSE strips"
maintainer = "Smart Cache"
type = "add-on"
tags = ["Sequencer"]
blender_version_min = "4.4.0"
license = ["SPDX:GPL-3.0-or-later"]

[permissions]
files = "Read/write cache files to disk"
